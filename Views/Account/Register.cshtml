<style>
    .error-container {
        text-align: center;
        padding: 20px;
        border: 2px solid #b36b00; /* warm gold */
        background-color: #f9f3e7; /* very light beige */
        color: #4a3700; /* dark warm brown */
        max-width: 420px;
        margin: 40px auto;
        font-family: 'Roboto', sans-serif;
        border-radius: 8px;
    }

        .error-container p {
            font-weight: 600;
            font-size: 1.1rem;
            margin-bottom: 20px;
            line-height: 1.4;
        }

    .btn-login {
        padding: 12px 28px;
        background-color: #b36b00; /* warm gold */
        color: #f9f3e7; /* light beige */
        border: none;
        border-radius: 6px;
        cursor: pointer;
        font-size: 1rem;
        font-weight: 600;
        transition: background-color 0.3s ease;
    }

        .btn-login:hover {
            background-color: #8c5600; /* darker warm gold */
        }
</style>

<div class="error-container">
    <p>Something went wrong. Please check your data and try signing up again.</p>
    <button class="btn-login" onclick="location.href='/account/login'">Go to Login</button>
</div>
